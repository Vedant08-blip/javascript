//Destructuring and JSON API
//A basic example of destructuring in js...
const person = {
  name: 'Vedant',
  age: 21,
  city: 'Vadodra'
};

// Destructuring
const { name, age } = person;
console.log(name); // Veadnt
console.log(age);  // 21
//Need to do more in this chapter!