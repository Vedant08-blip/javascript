//let str = " HelloWorld ";
console.log(str.trim().toUpperCase().slice(0, 5));
// Explanation:
// 1. trim() removes whitespace from both ends of the string, resulting in "HelloWorld".
// 2. toUpperCase() converts the string to uppercase, resulting in "HELLOWORLD".
// 3. slice(0, 5) extracts the first 5 characters, resulting in "HELLO".
// Final output: "HELLO"

 //let str = "JavaScript";
console.log(str.slice(1, 4)); 
// Explanation:
// 1. slice(1, 4) extracts characters from index 1 to index 3 (4 is not included).
// 2. The characters at these indices are "a", "v", and "a".
// Final output: "ava"






